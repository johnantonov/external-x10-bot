import { users_db } from "./models/users";


export async function startImport() {
  const usersData = '';

  console.log('import unpreparing')

  // for (const data of usersData) {
  //   await users_db.upsertUserFromJson(data);
  // }
}